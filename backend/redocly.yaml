extends:
  - recommended

# This API currently has no authentication and returns arrays (no 4xx) for
# list endpoints; the following opinionated rules are intentionally relaxed.
rules:
  security-defined: off
  operation-4xx-response: off
  info-contact: off
  info-license: off
